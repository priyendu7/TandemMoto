# state

Shared command/state protocol (protobuf or JSON) over a persistent socket,
separate from the audio and file-transfer sockets. Carries playback commands
(play/pause/skip/seek) and mic-mode state between phones.

Development plan: Phase 1-3.
